'use strict'

const { test, trait } = use('Test/Suite')('Bill tests');

test('fails to create bill without user authentication', async() => {

});

test('fails to confirm payment without allowed solicitation', async() => {
  
});

test('successfully create new bill', async() => {
  
});

